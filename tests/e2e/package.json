{
  "name": "the-ledger-e2e",
  "private": true,
  "description": "Playwright E2E for the mobile-first PWA. Run on demand against a live SPA.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
